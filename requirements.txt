# ============================================================
# requirements.txt
# Dépendances PRODUCTION — Raspberry Pi
#
# Installation recommandée:
# pip install --upgrade pip setuptools wheel
# pip install --no-cache-dir -r requirements.txt
#
# Important GPIO:
# - rpi-lgpio fournit le module RPi.GPIO (shim de compatibilité)
# - ne pas installer RPi.GPIO en même temps
# ============================================================

# Caméra Raspberry Pi
picamera2==0.3.31

# Traitement image
opencv-python==4.10.0.84
numpy==1.26.4

# Deep Learning - Anti-Spoofing (PyTorch pour Raspberry Pi)
# Note: PyTorch 2.0.1 requires Python <3.12
# For Python 3.12+ (Codespace) use 2.2.0+, for Raspberry Pi use 2.0.1
# Installation pour RPi: pip install torch==2.0.1 torchvision==0.15.2 --break-system-packages
torch==2.2.0
torchvision==0.17.0

# GPIO (capteur PIR, LED, buzzer)
rpi-lgpio==0.6
lgpio==0.2.2.0

# Envoi HTTP vers le serveur Django
requests==2.32.3

# Calcul distance pour liveness (EAR)
scipy==1.11.4

# Variables d'environnement
python-dotenv==1.0.1
